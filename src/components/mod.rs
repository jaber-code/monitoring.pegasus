//! Reusable UI building blocks.
//!
//! * [`layout`]  – app chrome shared by every screen (sidebar, topbar, shell).
//! * [`catalog`] – the dashboard-catalog widgets used by the home screen.
//! * [`icons`]   – small inline-SVG icon components (no icon-font dependency).

pub mod catalog;
pub mod icons;
pub mod jobs;
pub mod layout;
